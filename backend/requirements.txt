fastapi==0.115.6
uvicorn[standard]==0.32.1
numpy>=1.24.0,<2
scikit-learn>=1.3.0
#
# Speaker diarization stack (VAD + embeddings)
# - Silero VAD is loaded via torch.hub (no extra PyPI package needed)
# - SpeechBrain provides speaker embeddings for clustering
torch>=2.0.0
torchaudio>=2.0.0,<2.2.0
speechbrain>=0.5.16
#

