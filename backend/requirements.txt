fastapi==0.115.6
uvicorn[standard]==0.32.1
numpy>=1.24.0,<2
scikit-learn>=1.3.0
# Optional (needs C++ Build Tools on Windows): pip install webrtcvad Resemblyzer
# These are now part of the default requirements so multi-speaker diarization
# works out of the box. On Windows, make sure you have Microsoft C++ Build
# Tools installed before installing this requirements file.
webrtcvad==2.0.10
Resemblyzer>=0.1.4
